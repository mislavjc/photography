{
  "name": "photos",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "dev": "portless photography pnpm --filter web dev",
    "build": "turbo build",
    "start": "turbo start",
    "lint": "turbo lint",
    "lint-staged": "lint-staged",
    "typecheck": "turbo typecheck",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "knip": "knip",
    "prepare": "husky",
    "infra:deploy": "bun alchemy.run.ts",
    "infra:destroy": "bun alchemy.run.ts --destroy",
    "infra:dev": "alchemy dev alchemy.run.ts"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.4.2",
    "@commitlint/config-conventional": "^20.4.2",
    "@vitest/coverage-v8": "^4.0.18",
    "alchemy": "^0.83.3",
    "husky": "^9.1.7",
    "knip": "^5.85.0",
    "lint-staged": "^16.3.1",
    "prettier": "^3.8.1",
    "turbo": "^2.8.12",
    "vitest": "^4.0.18"
  },
  "lint-staged": {
    "*.{md,json,yml,yaml}": "prettier --write",
    "apps/web/**/*.{js,jsx,ts,tsx}": "pnpm --filter web exec eslint --fix",
    "apps/{cli,search-worker}/**/*.ts": "prettier --write"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp",
      "unrs-resolver",
      "workerd"
    ],
    "overrides": {
      "@types/react": "19.2.7",
      "@types/react-dom": "19.2.3"
    }
  }
}
